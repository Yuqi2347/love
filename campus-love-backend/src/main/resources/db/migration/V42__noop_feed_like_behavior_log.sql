-- =====================================================================
-- V42：动态点赞纳入行为日志（FEED_LIKE）与画像类目偏好汇总
--
-- 【无 DDL】应用层使用已有表 t_user_behavior_log：
--   behavior_type 新增取值 'FEED_LIKE'（VARCHAR(30) 已足够）
--
-- 本文件仅作版本占位与运维说明；执行不会改变任何表结构或数据。
-- 全新环境以 schema.sql 为准（已含 t_user_behavior_log）。
-- =====================================================================

SELECT 1 AS v42_schema_unchanged;
